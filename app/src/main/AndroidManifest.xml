<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!-- Wenn wir WIFI-Location und GPS-Location brauchen, d.h. Router und Antenne -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> <!-- Needed only if your app targets Android 5.0 (API level 21) or higher. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> <!-- Needed only if your app targets Android 5.0 (API level 21) or higher. -->
    <uses-feature android:name="android.hardware.location.gps"/>

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name">
        <activity android:name=".ConstraintActivity">
        </activity>
        <activity android:name=".TableActivity"
            android:theme="@style/Theme.AppCompat.Light">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ButtonActivity" android:theme="@style/Theme.MaterialComponents.Light">
        </activity>
        <activity android:name=".ListViewActivity">
        </activity>
        <activity android:name=".ExpandableListViewActivity">
        </activity>
        <activity android:name=".HardwareActivity">
        </activity>
        <activity android:name=".GridViewActivity">
        </activity>
        <activity android:name=".DynamicActivity">
        </activity>
        <activity android:name=".GpsActivity">
        </activity>
        <activity android:name=".ListViewActivity2Row"
            android:theme="@style/Theme.AppCompat.Light">
        </activity>
        <activity android:name=".RecyclerViewActivity">
        </activity>
        <activity android:name=".MeldungenActivity"
            android:theme="@style/Theme.AppCompat.Light">
        </activity>
    </application>

</manifest>